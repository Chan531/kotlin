server:
  port: 8081

spring:
  data:
    mongodb:
      uri: mongodb://root:1234@localhost:27017/boarddb?authSource=admin

  jackson:
    property-naming-strategy: SNAKE_CASE

logging:
  level:
    root: INFO
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
